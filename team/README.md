# Team pages

`index.qmd` is the team overview. Each individual profile is a sibling `.qmd` file, currently `jake-piekarski.qmd` and `eva-slason.qmd`.

When adding a profile, create the profile page, add its portrait to `images/`, and add a matching profile card to `team/index.qmd`. Run a full render to verify the overview and individual page links.
